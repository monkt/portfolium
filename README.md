# portfolium: localhost setup

1. download or clone repository
2. `cd` into `portfolium` directory
3. `npm install` to install dependencies
4. `node server` to initialize server
5. navigate browser to *localhost:3000*
